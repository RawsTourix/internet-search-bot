# v0.4-input-runtime — IR-10 automated release acceptance

Status: **AUTOMATED PASS / LIVE PENDING**

This report records the automated IR-10 release boundary only. It does **not** declare IR-10 complete and does **not** change the canonical `v0.4-input-runtime = partial` status. Maintainer-controlled real Telegram / LLM / safe read-only MCP acceptance remains mandatory before canonical documentation or PR stage status may move to implemented/validated.

## Boundary

- Repository: `RawsTourix/internet-search-bot`
- Branch: `feat/v0.4-input-runtime`
- Target: `feature`
- PR: `#6` (open, draft)
- Starting HEAD: `1f16ede96d76de213433884ce888fbd8007937c9`
- IR-9 code boundary: `068f8f6682e7b7b805b60dbb640b53b671cc8565`
- Automated IR-10 code/test/workflow boundary: `315ccd9e9fe6ab457bb39e9529774453fe4ad894`
- Current feature base SHA tested by PR workflow: `59d23c5fb6b69c3916a3cd94bcde914acf81aa4c`
- GitHub PR merge test SHA: `867af57ec36c89c2cd17dd5e8d6cabe570d52098`
- IR-10 commits after starting HEAD at automated boundary: `26`
- Ancestry: starting HEAD is the merge base; automated boundary is `26` commits ahead / `0` behind starting HEAD.
- Production `src/` changes in IR-10: **0**.
- History rewrite / reset / rebase / squash / force push: **none**.

Net IR-10 changed paths at the automated boundary:

- `.github/workflows/validate-input-runtime.yml`
- `reports/v0.4-input-runtime-ir10-acceptance.md`
- `reports/v0.4-input-runtime-ir10-acceptance.json`
- `tests/conftest.py`
- `tests/input_runtime_ir10_external_guard.py`
- `tests/test_input_runtime_ir10_capacity_roast.py`
- `tests/test_input_runtime_ir10_emission_roast.py`
- `tests/test_input_runtime_ir10_live_shape.py`
- `tests/test_input_runtime_ir10_process_restart.py`
- `tests/test_input_runtime_ir10_release.py`
- `tests/test_input_runtime_ir5_telegram.py`

No production defect requiring a `src/` fix was found. Failures encountered while building the acceptance harness were test/CI-environment defects and were made deterministic before correction; fixed seeds were retained.

## Final automated CI

GitHub Actions environment:

- workflow: `Validate Input Runtime #761`
- run id: `31367804201`
- job id: `93389906743`
- result: `completed / success`
- OS: Ubuntu `24.04.4` / `ubuntu-24.04`
- Python: CPython `3.12.13`
- pytest: `9.1.1`
- workflow permission: `contents: read` (`metadata: read` supplied by GitHub)
- job timeout: `10 minutes` — unchanged
- checkout: PR merge SHA `867af57ec36c89c2cd17dd5e8d6cabe570d52098`
- total job wall time: about `5m25s`, including dependency setup

Companion workflow:

- `Validate v0.4 file artifacts PR #841`
- run id: `31367804207`
- result: `completed / success`

The PR workflow checked out a synthetic merge commit against the current `feature` base, so the release evidence is not based on a stale isolated head.

## Automated results

| Gate | Result | Duration |
|---|---:|---:|
| `python -m compileall -q src` | success | ~0.61s |
| focused IR-8 | 49 passed, 0 failed, 0 skipped | 2.65s |
| focused IR-9 | 101 passed, 0 failed, 0 skipped | 1.27s |
| focused IR-10 | 25 passed, 0 failed, 0 skipped | 35.44s |
| seeded IR-10 release roast | 48 passed, 0 failed, 0 skipped; 7 deterministic non-roast cases deselected by selector | 93.21s |
| input-runtime + config regression | 562 passed, 0 failed, 0 skipped | 54.50s |
| full repository baseline | 1332 passed + 108 subtests passed, 0 failed, 0 skipped | 112.30s |

The full repository command is ordinary pytest with no test exclusion. Safe dummy environment values only satisfy existing fail-closed import-time configuration; they are not external credentials:

```text
AGENT_CONFIG_PATH=src/api/mcp.config.example
WEBHOOK_SECRET=<safe CI dummy>
TELEGRAM_FILE_PROVIDER_URL=http://127.0.0.1:8001
TELEGRAM_FILE_PROVIDER_TOKEN=<safe CI dummy>
python -m pytest -q --timeout=30 --timeout-method=thread
```

## Flake investigation and bounded-CI decision

A mandatory report-head rerun exposed one reproducible **acceptance-harness performance flake**, not a semantic/runtime failure:

- failing workflow: `Validate Input Runtime #759`, run id `31367446449`;
- production/test logic was unchanged from the previously green boundary;
- the 240-step runtime model hit the per-test `30s` timeout while the filesystem repository was inside a normal durable `os.fsync(directory_fd)` call;
- there was no invariant mismatch, deadlock trace, network call, or production exception;
- the same first fixed seed already passed the 180-step focused model on that slower hosted runner;
- no seed was removed and no timeout was increased.

Resolution: release runtime repetitions were reduced from `240` to `180` transitions per seed, preserving the same 12 published seeds, three-session interleaving, operation set and per-step invariants. The emission model remains `120` transitions per seed. This yields `3600` generated model transitions and keeps the job timeout at 10 minutes and per-test timeout at 30 seconds.

The bounded matrix then passed in `#761` on the stricter unchanged timeout. This is the release CI boundary used by this report.

## External-call guard

Focused IR-10 and release-roast commands load `tests.input_runtime_ir10_external_guard`. It fences real IPv4/IPv6 socket connects, the production Agent transport entry point, real LLM calls, MCP connect/tool calls and Telegram `Bot._post`.

Both final guarded commands ended with exactly:

```json
{
  "agent_cycle": 0,
  "internet": 0,
  "llm": 0,
  "mcp_network": 0,
  "telegram": 0,
  "web_external": 0
}
```

No real LLM, MCP network, Telegram, Web or Internet call is part of the automated IR-10 gate.

## Contract-to-test audit

IR-10 reuses existing IR-1—IR-9 deterministic suites as canonical contract evidence and adds cross-stage tests instead of duplicating every isolated regression.

| Contract class | Existing deterministic authority | IR-10 cross-stage evidence | Random/restart evidence |
|---|---|---|---|
| Admission / exactly-once / sequence | admission, IR-2 identity/index recovery | running burst, exact capacity, duplicate-zero-capacity | multi-session model |
| Checkpoint / accepted-at-entry / FIFO | checkpoint, checkpoint crash recovery, IR-3/IR-4 | running burst, pause/continue target, live-shaped mixed batch | multi-session model + repository reopen |
| Tool protocol | IR-3 contract gaps, IR-4 blocking contracts | closed multi-tool block before additions | protocol invariant after runtime-model steps + IR-8 recovery rejection |
| Pause / continue | IR-5 state/race matrix | frozen pre-continue target + late input | randomized pause/continue/reset |
| Reset / generation fencing | IR-5, IR-6 races, IR-8 reset recovery | crash during reset cleanup then restart/new cycle | randomized reset in runtime/emission models |
| AgentEmission | IR-6 emissions/delivery/races | route sanitization and lifecycle reuse | seeded emission/delivery model |
| Finalization / terminal | IR-7 finalization/handoff/output | IR-9 production-like integration + IR-8 partial-terminal ordering | restart/corruption suites retain terminal authority |
| Recovery | IR-8 focused 49 | repeated same-root recovery and independent Python processes | repository recreation in both models |
| Diagnostics | IR-9 diagnostics/integration | status during burst/paused flows | read-only invariant after runtime-model steps |
| Presentation | IR-9 projection/integration | production-like IR-9 acceptance | generation/terminal fencing retained by full regression |
| Session isolation | coordinator/IR-9 tests | same scope rules in synthetic flows | three interleaved sessions in both random models |
| Capacity | admission/checkpoint hardening | deterministic exact limit + duplicate + FIFO | 24 fixed-seed exact byte/count boundary scenarios |

## Deterministic cross-stage race matrix

`Event` means an explicit production-boundary `asyncio.Event`; `fault` means controlled durable-write failure; `lock/CAS` means exact-session coordination or compare-and-swap. `sleep()` is not used as a race oracle.

| Scenario | Barrier / authority | A linearizes first | B linearizes first | Result |
|---|---|---|---|---|
| admission ↔ duplicate | admission identity + lock/CAS | first creates one admission | duplicate observes same identity | PASS |
| admission ↔ terminal commit | IR-7 terminal barrier/Event | admitted input aborts stale terminal | terminal authority makes late input a new cycle | PASS |
| admission ↔ reset | generation/session lock | input belongs to old generation then reset fences it | reset advances generation before later admission | PASS |
| admission ↔ continue | frozen target/session lock | admitted sequence enters frozen target | later sequence is beyond target | PASS |
| admission ↔ readiness | readiness Event | recovery reconciles before READY | ordinary admission cannot pass non-READY gate | PASS |
| admission ↔ shutdown | lifecycle/reservation | admitted runner is owned then cancelled | STOPPING prevents new runner authority | PASS |
| pause ↔ continue | control sequence/CAS | pause intent precedes continue | continue cannot fabricate pause completion | PASS |
| pause ↔ LLM block | semantic checkpoint | block finishes then pause applies | pause never splits atomic block | PASS |
| pause ↔ multi-tool block | tool-protocol boundary | closed tool block then pause | pause waits for block closure | PASS |
| pause ↔ terminal finalization | terminal authority | pause wins before finalization | terminal prevents reopening | PASS |
| continue ↔ late input | immutable resume target | pre-continue queue drains | post-continue input waits | PASS |
| continue ↔ reset | generation/control authority | continue remains old-generation intent | reset fences old continue | PASS |
| reset ↔ terminal | generation/terminal authority | reset fences stale terminal work | terminal cycle remains terminal | PASS |
| reset ↔ active apply | claim/snapshot generation fence | old apply commits then is fenced | stale apply is rejected | PASS |
| reset ↔ emission claim | emission generation fence | old claim reaches its durable disposition | reset-first prevents stale claim | PASS |
| claim ↔ cancellation | inbox claim + cancellation | claim is recoverable/fenced | cancellation prevents semantic apply | PASS |
| APPLYING ↔ cancellation | APPLYING + snapshot | snapshot authority repairs markers | incomplete apply is requeued/fenced | PASS |
| snapshot persistence ↔ marker persistence | checkpoint fault injection | snapshot repairs missing markers | markers cannot invent semantic state | PASS |
| new input ↔ accepted-at-entry | accepted watermark | captured input drains | later input waits next checkpoint | PASS |
| input ↔ compaction | protocol/checkpoint boundary | complete revision compacts | input waits for safe insertion | PASS |
| input ↔ assistant multi-tool block | protocol boundary | closed block then update | update never enters open block | PASS |
| WAITING candidate ↔ input/control | session/status authority | WAITING becomes durable then reply resumes | admitted input/control prevents stale waiting authority | PASS |
| emission READY ↔ cancellation | READY + generation fence | cancellation converts/fences old READY | stale emitter cannot regain authority | PASS |
| claim ↔ competing claim | claim token + lease/CAS | first token owns DELIVERING | reverse ordering gives rival ownership | PASS |
| claim ↔ terminal | terminal/emission authority | earlier claim is reconciled | terminal-first blocks stale claim | PASS |
| claim ↔ reset | generation fence | old claim becomes durable terminal/unknown disposition | reset-first blocks stale claim | PASS |
| receipt ↔ lost response | immutable receipt identity | retry replays receipt | no second logical delivery | PASS |
| receipt ↔ reset | receipt + generation fence | receipt remains evidence | reset cannot restore old current authority | PASS |
| expired claim ↔ recovery | lease expiry | DELIVERING becomes UNKNOWN | UNKNOWN is never rearmed READY | PASS |
| result persistence ↔ late input | finalization + watermark | stale result cannot terminalize over accepted input | terminal authority classifies later input correctly | PASS |
| OUTPUT_READY ↔ late input | finalization/handoff | OUTPUT_READY alone is nonterminal | late input follows IR-7/IR-8 ordering | PASS |
| handoff completion ↔ terminal projection | COMPLETED prerequisite | completion enables terminal convergence | projection cannot precede completion | PASS |
| terminal commit ↔ output claim | output barrier/Event | terminal enables claim | pre-terminal claim rejects | PASS |
| terminal commit ↔ emission claim | terminal/generation fence | terminal fences stale emission | valid earlier delivery evidence stays immutable | PASS |
| terminal commit ↔ reset | terminal/generation authority | old terminal stays terminal | new generation cannot reopen it | PASS |
| recovery ↔ new admission | readiness + session authority | recovery repairs before READY | admission cannot race ahead | PASS |
| recovery ↔ control request | readiness | recovered state receives control | pre-READY control rejects | PASS |
| runner installation ↔ READY | readiness Event | reservation installed before READY | READY never precedes required ownership | PASS |
| recovered runner ↔ new addition | reservation + wake | same recovered cycle receives input | no second runner/cycle | PASS |
| shutdown ↔ recovered runner | task registry/lifecycle | shutdown cancels runner | next startup installs deterministically | PASS |
| shutdown ↔ admitted runner | coordinator reservation | owned task is cancelled | STOPPING blocks new ownership | PASS |
| status ↔ admission | diagnostics boundary | coherent pre/post snapshot | never mutates admission | PASS |
| status ↔ control | diagnostics boundary | coherent pre/post control state | no control allocated by status | PASS |
| status ↔ terminal commit | terminal read authority | coherent nonterminal/terminal view | no mixed stale authority | PASS |
| queued presentation ↔ APPLIED presentation | presentation revision fence | APPLIED supersedes queued | stale queued update cannot overwrite | PASS |
| old-generation edit ↔ reset | generation fence | old edit loses authority | new generation wins | PASS |
| progress edit ↔ terminal presentation | terminal presentation barrier | terminal suppresses later progress | progress-first is superseded | PASS |

## Restart and crash evidence

IR-10 uses two levels: fresh repository/service/coordinator objects over the same filesystem root, and fresh Python interpreters without inherited ContextVars, task registries, lock registries or wake events.

IR-10 additions:

1. RUNNING persisted in process A → process B opens same root → same cycle recovered as safe interrupted/auto-resume authority with one admission.
2. WAITING persisted in process A → process B recovers exact cycle/question/context → independent process C repeats with identical counts.
3. WAITING same-root recovery repeated three times with identical cycle/question/admission/context-revision authority.
4. WAITING + queued addition → restart → later user reply → `RESUME_WAITING` same cycle → FIFO apply.
5. Reset generation advances → cleanup fault → restart → cleanup reconciliation once → later input starts only new generation.

Focused IR-8 additionally covers RUNNING pre-handoff, HANDED_OFF/AMBIGUOUS, PAUSE_REQUESTED, PAUSED_BY_USER, WAITING_USER, READY/DELIVERING/UNKNOWN emissions, PREPARED/RESULT_PERSISTED finalization, partial terminal, terminal authority, readiness, runner installation and shutdown ownership.

| Durable point | Expected recovery disposition | Evidence | Result |
|---|---|---|---|
| admission record / index | reuse/repair identity, no duplicate | IR-2 crash/index recovery | PASS |
| session accepted watermark | repair lag / reject impossible ahead | IR-8 admission recovery | PASS |
| inbox relation | rebuild missing derived relation | IR-8 admission recovery | PASS |
| claim / APPLYING | requeue or repair markers from snapshot authority | IR-8 claim recovery | PASS |
| context revision / snapshot | reuse immutable revision/snapshot | checkpoint crash recovery | PASS |
| APPLIED markers | finish markers from snapshot | IR-8 claim recovery | PASS |
| control record / watermark | sequence/idempotency preserved | IR-5 persistence/races | PASS |
| continue frozen target | remains immutable | IR-5 + IR-10 pause/continue | PASS |
| reset generation advance | cleanup resumes, no second increment | IR-8 + IR-10 reset crash | PASS |
| AgentEmission READY / identity | survives/reuses exact logical identity | IR-6/IR-8 + random model | PASS |
| DELIVERING / receipt | ambiguity → UNKNOWN; receipt replay idempotent | IR-6/IR-8 + random model | PASS |
| HANDED_OFF | conservative AMBIGUOUS, no blind replay | IR-8 restart states | PASS |
| final result / RESULT_PERSISTED | exact result reused | IR-8 finalization recovery | PASS |
| OutputBatch / OUTPUT_READY | no claim before terminal barrier | IR-7/IR-8 | PASS |
| handoff COMPLETED | never downgraded; enables convergence | IR-7/IR-8 | PASS |
| terminal snapshot/session/marker | exact authority or controlled corruption failure | IR-8 | PASS |

## Corruption / authority recovery

- selected IR-2 derived identity/index recovery cases: `30`
- IR-8 core cycle-authority corruption cases: `7`
- combined selected corruption/authority cases: `37`

Core IR-8 cases:

| Case | Expected | Result |
|---|---|---|
| missing cycle pointer with agreeing immutable records | deterministic rebuild | PASS |
| dangling cycle pointer with agreeing immutable records | deterministic rebuild | PASS |
| divergent durable cycle/session authority | controlled startup failure | PASS |
| duplicate authoritative admission sequence | controlled startup failure | PASS |
| missing committed batch referenced by admission | controlled startup failure | PASS |
| missing required active context revision | controlled startup failure | PASS |
| incompatible nonterminal handoffs | controlled startup failure | PASS |

No newest/mtime/majority winner is used for immutable contradiction. Derived state is repaired only when immutable authority agrees.

## Seeded randomized / model-based roast

Published fixed seed set:

```text
20260810
104729
8675309
424242
1
7
42
271828
314159
1618033
999983
2147483647
```

No failing seed was removed.

Runtime model:

- 12 seeds;
- 3 sessions per seed;
- 180 generated operations per seed;
- **2160 runtime transitions**;
- admission, duplicate, checkpoint, pause, continue, reset, diagnostics, repository recreation;
- invariants after each step include contiguous sequences, accepted >= applied, control idempotency, generation/snapshot authority, tool protocol, diagnostics non-mutation and session isolation.

Emission/delivery model:

- same 12 seeds with deterministic domain-separated RNG;
- 3 sessions per seed;
- 120 generated operations per seed;
- **1440 emission/delivery transitions**;
- emission/replay, claim/competing claim, receipt+reopen, deterministic failure, UNKNOWN ambiguity, expiry and generation reset;
- invariants after each step include one logical emission identity, route/session isolation, no credential persistence, one claim token, terminal/UNKNOWN not READY/pending and old-generation fencing.

Capacity model:

- same 12 seeds;
- 24 exact-boundary scenarios: 12 byte + 12 batch-count;
- exact limit accepted;
- duplicate consumes no second capacity;
- `+1` rejected by the correct limit;
- bounded checkpoint revision count asserted for byte scenarios.

Total generated model transitions: **3600**. Final semantic failures: **0**. Unreproduced failures: **0**. Remaining flaky seeds: **0** after bounding the filesystem-heavy repetition count. The previous 240-step timeout event is retained in this report rather than hidden.

## Synthetic acceptance

| Scenario | Core operations | Restart count | Final durable oracle | External side effects | Result |
|---|---|---:|---|---:|---|
| A — running burst | initial cycle → 3 additions → status → closed multi-tool checkpoint | 0 | one cycle, FIFO sequences, all applied | 0 | PASS |
| B — pause/resume | pause → addition → PAUSED → continue target → late addition | 0 | same cycle/gen; frozen target exact; late waits | 0 | PASS |
| C — WAITING + restart | WAITING + queued addition → restart → reply | 1 | same cycle/question/context; FIFO apply | 0 | PASS |
| D — reset/restart | active work → generation advance → injected cleanup crash → restart → later new input | 1 | old cycle fenced; new input only next generation | 0 | PASS |
| E — partial terminal | HANDED_OFF and COMPLETED partial-terminal orderings | IR-8 recovery | completed old cycle never reopens | 0 | PASS |
| live-shaped mixed batch | 30 artifact refs + 7 message parts + 64 KiB payload + follow-up | 0 | ordered projection; coherent durable artifact refs | 0 | PASS |
| multi-session marathon | 3 interleaved sessions, controls, resets, checkpoints, diagnostics, reopen | repeated object recreation | no cross-session authority bleed | 0 | PASS |
| production-like IR-9 integration | admission/checkpoint/control/diagnostics/projection/finalization/output | IR-8 around it | coherent terminal/client projection | 0 | PASS |

## Defect classification

### A — blocking production defect

None found.

### B — required production hardening

None found.

### Acceptance/test harness corrections

- invalid future fake clock vs repository-owned UTC cleanup timestamp;
- inputs intended to arrive after restart accidentally visible before recovery;
- ready-list oracle requested a limit above the repository public bound;
- process-global IR-7 test bridge leaked into standalone output-store tests;
- synthetic Telegram host discovery depended on full-suite import order;
- full-repository import-time configuration ordering was not process-stable;
- repeated READY-index oracle scans were optimized without changing invariants;
- 240-step filesystem-heavy release case could exceed the strict 30s per-test timeout on a slow hosted runner; bounded to 180 with seeds/invariants unchanged.

### C — deferred / outside v0.4-input-runtime

- Telegram edited-message history rewind
- PostgreSQL / SQLAlchemy / Alembic
- Redis
- distributed workers / leases
- distributed runtime
- scheduler
- `AgentRun` / `TaskRun`
- parallel branches
- fork/join
- new durable global event bus

### D — cosmetic

No cosmetic issue blocks acceptance.

## Maintainer live gate — PENDING

Live acceptance was **not** executed by GitHub Actions and was **not** simulated as a substitute for real service behavior.

The maintainer must test exact automated code/test/workflow SHA `315ccd9e9fe6ab457bb39e9529774453fe4ad894` or a later **report-only** descendant whose production/test/workflow content is unchanged from this SHA. Use no uncommitted production changes, preferably a dedicated Telegram test chat and isolated storage root/profile.

Required live scenarios:

1. **Real E2E basic** — Telegram input → ingress → committed batch → admission → AgentCycle → real LLM → safe read-only MCP/search/read → finalization → exactly one final Telegram output. Check initial acknowledgement, one active cycle, one final delivery, no duplicate.
2. **`/status`** — during RUNNING and after terminal. Must be read-only, coherent, create no input/control/cycle and not interrupt work.
3. **Running additions** — send at least two follow-ups while one real task is active. Confirm same cycle, FIFO acknowledgements/APPLIED projection and final answer accounting for additions.
4. **`/stop`** — distinguish command accepted from later durable `PAUSED_BY_USER`. During a tool block, the block must close with valid assistant/tool history and no next semantic block starts.
5. **Input while paused** — ordinary input is durably accepted/queued; cycle remains paused; no implicit continue or runner wake; `/status` reflects it.
6. **`/continue`** — same cycle id; pre-continue queued input is in frozen target and applied before the next meaningful resumed LLM step; late post-continue input waits for a later checkpoint.
7. **Semantic AgentEmission** — only if real production profile exposes `send_user_message`. Observe one semantic intermediate Telegram message, no duplicate, cycle continues and final output is separate. If not exposed, record the factual reason; do not alter product prompts/semantics.
8. **Safe restart** — reach actual `PAUSED_BY_USER`, stop app, restart same durable root, `/status` shows same paused cycle/context, then `/continue` resumes same cycle without duplicate or blind replay.
9. **Optional natural WAITING restart** — only if normal production behavior can reach `WAITING_USER`: restart, preserve same question/context, reply and `RESUME_WAITING` same cycle. Do not change prompts to force it.

Live safety:

- read/search/retrieve/status external operations only;
- no email send, purchase, calendar mutation, deletion or irreversible action;
- no tokens, keys, chat ids, private endpoint credentials, raw private user text, raw prompts or raw tool contents in committed evidence;
- record scenario id, exact SHA, PASS/FAIL, lifecycle summary, services used and duplicate side-effect count.

If any live scenario fails, IR-10 remains incomplete. Reproduce offline where possible, fix minimally, retain/add regression, obtain a new automated green SHA and rerun affected live scenarios. Live evidence from an older production SHA is invalid after a production fix.

## Automated conclusion

**IR-10 automated acceptance = GREEN.**

**Maintainer live gate = PENDING.**

Therefore this report deliberately does **not** state `IR-10 implemented and validated` and does **not** state `v0.4-input-runtime implemented and validated`. Canonical docs and PR #6 IR-10 checkbox remain unchanged until the real maintainer live gate passes.
