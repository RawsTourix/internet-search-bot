# v0.4-input-runtime — IR-10 automated release acceptance

Status: **AUTOMATED PASS / LIVE PENDING**

This report records the automated IR-10 release boundary only. It does **not** declare IR-10 complete and does **not** change the canonical `v0.4-input-runtime = partial` status. Maintainer-controlled real Telegram / LLM / safe read-only MCP acceptance remains mandatory before canonical documentation or PR stage status may be moved to implemented/validated.

## Boundary

- Repository: `RawsTourix/internet-search-bot`
- Branch: `feat/v0.4-input-runtime`
- Target: `feature`
- PR: `#6` (open, draft)
- Starting HEAD: `1f16ede96d76de213433884ce888fbd8007937c9`
- IR-9 code boundary: `068f8f6682e7b7b805b60dbb640b53b671cc8565`
- Automated IR-10 head SHA: `8a299202e7a9e5040c9b463a2813aabf99e4612a`
- Current feature base SHA tested by PR workflow: `59d23c5fb6b69c3916a3cd94bcde914acf81aa4c`
- GitHub PR merge test SHA: `21be48893c5ec29bac43a45b74a16e8eb2951d0c`
- IR-10 commits after starting HEAD: `23`
- Ancestry: starting HEAD is the merge base; automated head is `23` commits ahead / `0` behind starting HEAD.
- Production `src/` changes in IR-10: **0**.
- History rewrite / reset / rebase / squash / force push: **none**.

Net IR-10 changed paths at the automated head:

- `.github/workflows/validate-input-runtime.yml`
- `tests/conftest.py`
- `tests/input_runtime_ir10_external_guard.py`
- `tests/test_input_runtime_ir10_capacity_roast.py`
- `tests/test_input_runtime_ir10_emission_roast.py`
- `tests/test_input_runtime_ir10_live_shape.py`
- `tests/test_input_runtime_ir10_process_restart.py`
- `tests/test_input_runtime_ir10_release.py`
- `tests/test_input_runtime_ir5_telegram.py`

No production defect requiring a `src/` fix was found. The failures encountered while building the acceptance harness were test/CI-environment defects: an invalid future fake clock, pre-committing inputs that were supposed to arrive after restart, an over-large ready-list oracle limit, process-global test bridge leakage, Telegram synthetic-host import-order leakage, and full-suite import-time configuration ordering. Each was made deterministic and corrected without weakening production authority.

## Environment and CI

GitHub Actions environment for the release run:

- workflow: `Validate Input Runtime #755`
- run id: `31366811919`
- result: `completed / success`
- OS: Ubuntu `24.04.4` / `ubuntu-24.04`
- Python: CPython `3.12.13`
- pytest: `9.1.1`
- workflow permission: `contents: read` (`metadata: read` supplied by GitHub)
- job timeout: `10 minutes` — unchanged
- total job wall time: approximately `5m50s`, including dependency setup

Companion workflow:

- `Validate v0.4 file artifacts PR #838`
- run id: `31366811751`
- result: `completed / success`

The PR workflow checked out GitHub's synthetic merge commit `21be48893c5ec29bac43a45b74a16e8eb2951d0c`, so the evidence is against the current `feature` base rather than a stale isolated head.

## Automated results

| Gate | Result | Duration |
|---|---:|---:|
| `python -m compileall -q src` | success | ~0.61s |
| focused IR-8 | 49 passed, 0 failed, 0 skipped | 2.63s |
| focused IR-9 | 101 passed, 0 failed, 0 skipped | 1.19s |
| focused IR-10 | 25 passed, 0 failed, 0 skipped | 34.85s |
| seeded IR-10 release roast | 48 passed, 0 failed, 0 skipped; 7 deterministic non-roast cases deselected by design | 117.47s |
| input-runtime + config regression | 562 passed, 0 failed, 0 skipped | 54.84s |
| full repository baseline | 1332 passed + 108 subtests passed, 0 failed, 0 skipped | 112.60s |

The full repository command is ordinary pytest with no test exclusion:

```text
AGENT_CONFIG_PATH=src/api/mcp.config.example
WEBHOOK_SECRET=<safe CI dummy>
TELEGRAM_FILE_PROVIDER_URL=http://127.0.0.1:8001
TELEGRAM_FILE_PROVIDER_TOKEN=<safe CI dummy>
python -m pytest -q --timeout=30 --timeout-method=thread
```

The dummy values only satisfy existing fail-closed import-time configuration. They do not provide real external credentials.

## External-call guard

Focused IR-10 and release-roast commands load `tests.input_runtime_ir10_external_guard`. It fences real IPv4/IPv6 socket connects, the production Agent transport entry point, real LLM calls, MCP connect/tool calls and Telegram `Bot._post`.

Both guarded commands ended with exactly:

```json
{
  "agent_cycle": 0,
  "internet": 0,
  "llm": 0,
  "mcp_network": 0,
  "telegram": 0,
  "web_external": 0
}
```

No real LLM, MCP network, Telegram, Web or Internet call is part of the automated IR-10 gate.

## Contract-to-test audit

IR-10 reuses the existing IR-1—IR-9 deterministic suites as canonical contract evidence and adds cross-stage cases instead of duplicating every isolated test.

| Contract class | Existing deterministic authority | IR-10 cross-stage evidence | Random/restart evidence |
|---|---|---|---|
| Admission / exactly-once / sequence | admission, IR-2 identity/index recovery | running burst, exact capacity, duplicate-zero-capacity | multi-session model |
| Checkpoint / accepted-at-entry / FIFO | checkpoint, checkpoint crash recovery, IR-3/IR-4 | running burst, pause/continue target, live-shaped mixed batch | multi-session model + repository reopen |
| Tool protocol | IR-3 contract gaps, IR-4 blocking contracts | closed multi-tool block before additions | protocol invariant after every runtime model step + IR-8 recovery rejection |
| Pause / continue | IR-5 state/race matrix | pre-continue frozen target + late input | randomized pause/continue/reset |
| Reset / generation fencing | IR-5, IR-6 races, IR-8 reset recovery | crash during reset cleanup then restart/new cycle | randomized generation reset in runtime and emission models |
| AgentEmission | IR-6 emissions/delivery/races | production-shaped route sanitization and lifecycle reuse | seeded emission/delivery model |
| Finalization / terminal | IR-7 finalization/handoff/output tests | IR-9 production-like integration + IR-8 partial-terminal ordering | restart/corruption suites retain terminal authority |
| Recovery | IR-8 focused 49 | repeated same-root recovery and independent Python processes | repository recreation in both state models |
| Diagnostics | IR-9 diagnostics/integration | status during burst/paused flows | read-only invariant after runtime-model steps |
| Presentation | IR-9 projection/integration | reused production-like IR-9 acceptance | generation/terminal fencing retained by full regression |
| Session isolation | IR-9 diagnostics and coordinator tests | same scope rules in synthetic flows | three interleaved sessions in both random models |
| Capacity | admission/checkpoint hardening | deterministic exact limit + duplicate + FIFO | 24 fixed-seed exact byte/count boundary scenarios |

## Deterministic cross-stage race matrix

The table records the canonical deterministic ordering evidence. `Event` means an explicit production-boundary `asyncio.Event`; `fault` means a controlled durable-write failure; `lock/CAS` means the production exact-session coordination / compare-and-swap linearization point. No `sleep()` is used as a race oracle.

| Scenario | Barrier / authority | A linearizes first | B linearizes first | Result |
|---|---|---|---|---|
| admission ↔ duplicate | admission identity + lock/CAS | first creates one admission | duplicate observes same identity | PASS, one admission |
| admission ↔ terminal commit | IR-7 terminal barrier/Event | admitted input aborts stale terminal | terminal authority makes late input a new cycle | PASS |
| admission ↔ reset | generation/session lock | admission belongs to old generation then reset fences it | reset advances generation, later admission targets new generation | PASS |
| admission ↔ continue | control target + session lock | queued sequence is in frozen target | later admission is beyond target | PASS |
| admission ↔ readiness | readiness Event | recovery reconciles committed input before READY | ordinary admission cannot pass non-READY gate | PASS |
| admission ↔ shutdown | coordinator reservation/lifecycle | admitted runner is owned then cancelled | STOPPING prevents new runner authority | PASS |
| pause ↔ continue | control sequence/CAS | pause effect may be acknowledged then continued | continue cannot invent pause completion | PASS |
| pause ↔ LLM block | semantic checkpoint boundary | completed block reaches pause checkpoint | pause cannot split atomic LLM block | PASS |
| pause ↔ multi-tool block | closed assistant/tool protocol boundary | complete tool block then pause | pause waits; no orphan tool result | PASS |
| pause ↔ terminal finalization | finalization/terminal authority | pause wins before terminal path continues | terminal authority prevents reopening | PASS |
| continue ↔ late input | frozen `resume_input_target` | pre-continue input drains on resume | post-continue input waits next checkpoint | PASS |
| continue ↔ reset | generation/control authority | continue remains old-generation intent | reset fences old continue | PASS |
| reset ↔ terminal | generation vs terminal authority | reset fences old terminal work | terminal cycle stays terminal; reset/new generation cannot resurrect it | PASS |
| reset ↔ active apply | claim/snapshot generation fence | apply commits under old generation then reset cancels old authority | reset causes stale apply rejection | PASS |
| reset ↔ emission claim | emission generation fence | old claim becomes terminal/unknown according delivery state | reset cancels READY / fences stale claimant | PASS |
| claim ↔ cancellation | durable inbox claim + cancellation cleanup | claim is recoverable/fenced | cancellation prevents semantic apply | PASS |
| APPLYING ↔ cancellation | APPLYING marker + snapshot authority | applied snapshot wins and markers repair | cancellation/recovery requeues or fences incomplete apply | PASS |
| snapshot persistence ↔ marker persistence | checkpoint fault injection | snapshot authority repairs missing markers | marker cannot create semantic state without snapshot authority | PASS |
| new input ↔ accepted-at-entry | session accepted watermark | input within captured target drains | later input waits next checkpoint | PASS |
| input ↔ compaction | protocol/checkpoint boundary | complete revision is compacted safely | new input waits until compacted block closes | PASS |
| input ↔ assistant multi-tool block | protocol boundary | closed block then update | update never enters open tool block | PASS |
| WAITING candidate ↔ input/control | waiting status/session lock | WAITING becomes durable then reply resumes same cycle | admitted reply/control prevents stale WAITING projection | PASS |
| emission READY ↔ cancellation | READY durable marker + generation fence | reset/cancel converts old READY | stale emitter cannot regain current authority | PASS |
| claim ↔ competing claim | claim token + lease/CAS | first token owns DELIVERING | reverse token owns; rival conflicts | PASS |
| claim ↔ terminal | terminal/output authority | claim outcome is fenced by terminal reconciliation | terminal-first cancels old READY/no new attempt | PASS |
| claim ↔ reset | generation fence | old claim becomes UNKNOWN/CANCELLED as required | reset-first blocks stale claim | PASS |
| receipt ↔ lost response | immutable receipt identity | retry replays same receipt | no second delivery attempt is allocated | PASS |
| receipt ↔ reset | receipt + generation fence | durable receipt remains evidence | reset prevents stale receipt from restoring current authority | PASS |
| expired claim ↔ recovery | lease expiry | expired DELIVERING becomes UNKNOWN | recovery never re-arms UNKNOWN as READY | PASS |
| result persistence ↔ late input | finalization state + accepted watermark | stale result cannot terminalize over admitted input | terminal/result authority makes later input a new cycle only when allowed | PASS |
| OUTPUT_READY ↔ late input | finalization/handoff authority | OUTPUT_READY alone is not terminal | late input follows IR-7/IR-8 ordering | PASS |
| handoff completion ↔ terminal projection | completed handoff prerequisite | COMPLETED permits terminal convergence | no terminal projection before completion | PASS |
| terminal commit ↔ output claim | explicit Event/output barrier | terminal first enables claim | pre-terminal claim is rejected | PASS |
| terminal commit ↔ emission claim | terminal/generation fence | terminal cancels/blocks stale emission claim | valid earlier delivery evidence remains immutable | PASS |
| terminal commit ↔ reset | terminal + generation authority | terminal cycle remains terminal | reset creates fenced next generation, never reopens old cycle | PASS |
| recovery ↔ new admission | readiness + exact-session authority | recovery repairs/reconstructs before READY | ordinary admission cannot race ahead of recovery gate | PASS |
| recovery ↔ control request | readiness gate | recovered state receives control after READY | control before READY is rejected | PASS |
| runner installation ↔ READY | explicit readiness Event | recovered runner reservation installed before READY | READY never precedes required runner ownership | PASS |
| recovered runner ↔ new addition | reservation + wake | same recovered cycle receives addition | no second runner/cycle allocated | PASS |
| shutdown ↔ recovered runner | task registry/lifecycle | shutdown cancels owned recovered runner | startup installs exactly one on next lifecycle | PASS |
| shutdown ↔ admitted runner | coordinator reservation | admitted task is cancelled/cleared | STOPPING blocks a new owned runner | PASS |
| status ↔ admission | diagnostics read boundary | either pre- or post-admission coherent snapshot | never mutates admission | PASS |
| status ↔ control | diagnostics read boundary | either pre- or post-control coherent state | no control sequence allocated by status | PASS |
| status ↔ terminal commit | terminal read authority | either nonterminal or terminal coherent projection | no stale mixed generation | PASS |
| queued presentation ↔ APPLIED presentation | presentation revision fence | APPLIED supersedes queued | stale queued update cannot overwrite newer revision | PASS |
| old-generation edit ↔ reset | generation fence | old edit loses current authority | reset/new generation presentation wins | PASS |
| progress edit ↔ terminal presentation | terminal presentation barrier | terminal version suppresses later progress | progress-first is superseded by terminal | PASS |

## Crash / restart evidence

IR-10 does not introduce a test-only production backdoor. Same-root restart uses new repository/service/coordinator objects over the real filesystem layout; cold restart uses fresh Python interpreters.

IR-10 additions:

1. RUNNING state persisted in process A → process B opens the same root → recovery yields the same cycle as safe `INTERRUPTED` / auto-resume disposition, with one admission.
2. WAITING state persisted in process A → independent process B recovers exact cycle/question/context → independent process C repeats the same recovery with identical admission/revision counts.
3. WAITING same-root recovery is repeated three times with identical cycle/question/admission/context-revision authority.
4. WAITING + queued addition → restart → later user reply → `RESUME_WAITING` to the same cycle → FIFO apply.
5. Reset generation advances durably → cleanup fault → restart → cleanup reconciliation completes once → later new input starts only the new generation.

Reused IR-8 focused evidence additionally covers RUNNING pre-handoff, HANDED_OFF/AMBIGUOUS, PAUSE_REQUESTED, PAUSED_BY_USER, WAITING_USER, READY/DELIVERING/UNKNOWN emission recovery, PREPARED/RESULT_PERSISTED finalization, partial terminal, terminal committed authority, runner installation, readiness and shutdown ownership.

### Durable crash points

| Durable point | Expected recovery disposition | Evidence family | Result |
|---|---|---|---|
| admission record | repair/reuse identity, no duplicate | IR-2 crash/index recovery | PASS |
| admission identity/index | deterministic derived repair or contradiction failure | IR-2 index recovery | PASS |
| session accepted watermark | repair lag / reject impossible ahead | IR-8 admission recovery | PASS |
| inbox relation | rebuild missing derived relation | IR-8 admission recovery | PASS |
| claim record | expired claim requeues if no semantic apply | IR-8 claim recovery | PASS |
| APPLYING marker | snapshot decides requeue vs finish markers | IR-8 claim recovery | PASS |
| context revision | immutable revision reused, no duplicate | checkpoint crash recovery | PASS |
| active snapshot | snapshot is semantic authority | checkpoint crash recovery | PASS |
| APPLIED markers | finish markers from authoritative snapshot | IR-8 claim recovery | PASS |
| control record / pending watermark | sequence/idempotency preserved | IR-5 persistence/race suites | PASS |
| continue frozen target | target remains immutable | IR-5 continue-target + IR-10 pause/continue | PASS |
| reset generation advancement | cleanup resumes without second increment | IR-8 reset recovery + IR-10 crash | PASS |
| AgentEmission READY | survives restart; wake failure does not erase it | IR-6/IR-8 emission recovery | PASS |
| emission identity index | same logical tool call reuses identity | IR-6 + seeded emission model | PASS |
| DELIVERING claim | expired ambiguity → UNKNOWN, no blind replay | IR-6/IR-8 delivery recovery | PASS |
| delivery receipt | immutable receipt replay/idempotency | IR-6 + seeded emission model | PASS |
| RuntimeHandoff HANDED_OFF | conservative AMBIGUOUS after restart | IR-8 restart states | PASS |
| final result / RESULT_PERSISTED | exact durable result reused | IR-8 finalization recovery | PASS |
| OutputBatch / OUTPUT_READY | no terminal claim until full barrier | IR-7/IR-8 finalization | PASS |
| RuntimeHandoff COMPLETED | never downgraded; enables terminal convergence | IR-7/IR-8 terminal ordering | PASS |
| terminal snapshot/session/marker | exact terminal authority or controlled corruption failure | IR-8 terminal authority/composition | PASS |

## Corruption / authority recovery

The acceptance combines the IR-2 derived identity/index permutation matrix with the IR-8 cycle-authority corruption core.

- IR-2 selected derived/index recovery cases: `30`
- IR-8 core cycle-authority corruption cases: `7`
- Combined selected corruption/authority cases: `37`

IR-8 core cases:

| Case | Authority sources | Expected | Actual |
|---|---|---|---|
| missing cycle pointer | agreeing immutable session/admission/snapshot authority | deterministic rebuild | PASS |
| dangling cycle pointer | agreeing immutable records, stale derived pointer | deterministic rebuild | PASS |
| divergent cycle/session authority | immutable sources disagree | controlled startup failure | PASS |
| duplicate authoritative admission session sequence | immutable sequence contradiction | controlled startup failure | PASS |
| missing committed batch referenced by admission | missing immutable committed input | controlled startup failure | PASS |
| missing required active context revision | immutable protocol/context gap | controlled startup failure | PASS |
| multiple incompatible nonterminal handoffs | incompatible immutable handoff authority | controlled startup failure | PASS |

No newest/mtime/majority winner is used for immutable contradiction. Derived state is repaired only when immutable authority agrees.

## Seeded randomized / model-based roast

Published release seed set, fixed before failures were observed:

```text
20260810
104729
8675309
424242
1
7
42
271828
314159
1618033
999983
2147483647
```

No failing seed was removed.

Runtime state-machine model:

- seeds: `12`
- sessions per seed: `3`
- generated operations per seed: `240`
- generated transitions: `2880`
- operations: admission, duplicate delivery, checkpoint apply, pause, continue, reset, diagnostics, repository recreation
- invariants after every step: contiguous session/cycle sequences, accepted >= applied, control idempotency/sequence, generation/snapshot authority, no shared current cycle across sessions, valid tool protocol, diagnostics non-mutation

Emission/delivery model:

- same 12 seeds (deterministically domain-separated RNG)
- sessions per seed: `3`
- generated operations per seed: `120`
- generated transitions: `1440`
- operations: semantic emission, exact replay, claim, competing claim, receipt + repository recreation, deterministic failure, UNKNOWN ambiguity, claim expiry, generation reset
- invariants after every step: one logical emission identity, route/session isolation, no credential persistence, one active claim token, terminal/UNKNOWN not READY or pending, old generation never regains READY/DELIVERING authority

Capacity model:

- same 12 seeds
- `24` exact-boundary scenarios (`12` byte + `12` batch-count)
- randomized limits/splits around exact configured boundaries
- exact limit accepted
- duplicate consumes no second capacity
- `+1` is rejected by the expected byte/count contract
- bounded checkpoint revision count is asserted for byte scenarios

Total generated runtime + emission state transitions: **4320**.

Final failures: **0**. Unreproduced failures: **0**. Flaky seeds: **0**.

## Synthetic acceptance

| Scenario | Operations / authority | Restart count | Final durable oracle | Automated external side effects | Result |
|---|---|---:|---|---:|---|
| A — running burst | one initial cycle → 3 additions → status → closed multi-tool checkpoint → FIFO bounded revisions | 0 | one cycle, session seq 1..4, cycle seq 0..3, all applied | 0 | PASS |
| B — pause/resume | pause → pre-continue addition → PAUSED → continue frozen target → late addition → next checkpoint | 0 | same cycle/generation; frozen target drains exactly; late input waits | 0 | PASS |
| C — WAITING + restart | WAITING + queued addition → restart → new reply → RESUME_WAITING | 1 | same cycle/question/context; queued + reply FIFO applied | 0 | PASS |
| D — reset/restart | active cycle/addition → generation advancement → injected cleanup crash → restart → later new input | 1 | old cycle CANCELLED/fenced; new input only generation+1/new cycle | 0 | PASS |
| E — partial terminal | HANDED_OFF partial terminal and COMPLETED partial terminal orderings | restart/recovery coverage in IR-8 | late input follows IR-7/IR-8; completed old cycle never reopens | 0 | PASS |
| live-shaped mixed batch | 30 artifact refs + 7 message parts + 64 KiB payload → repeated addition → checkpoint | 0 | projection preserves batch order/shape; durable artifact refs deduplicated coherently | 0 | PASS |
| multi-session marathon | 3 interleaved sessions with admissions, duplicates, controls, resets, checkpoints, diagnostics, repository recreation | many object recreations | no cross-session cycle/sequence/generation contamination | 0 | PASS |
| production-like IR-9 integration | admission/checkpoint/control/diagnostics/projection/finalization/output/terminal composition | existing restart suites around it | coherent terminal/client projection | 0 | PASS |

Temporary filesystem roots are pytest-owned and reclaimable. Recreated repositories do not inherit coordinator caches, wake events or task ownership. Fresh-interpreter cases additionally prove independence from module-local tasks, lock registries and ContextVars.

## Full-repository baseline findings

The first attempt to add `python -m pytest -q` exposed collection-order/configuration assumptions rather than a production runtime defect:

- process-level `AGENT_CONFIG_PATH` must be set before any module imports `src.api.config`;
- the existing fail-closed Telegram file-provider pair must be both present or both absent;
- standalone output-store tests must not inherit the process-global IR-7 production finalization bridge from an earlier `Api` composition;
- a synthetic Telegram host-shape test must not accidentally discover an already imported real host before its synthetic host.

The final gate sets the canonical example config at process start, uses safe dummy paired env values, and isolates only the legacy standalone test fixtures. Production fail-closed checks and production finalization barriers are unchanged.

Final full repository result: **1332 passed + 108 subtests passed, 0 failed, 0 skipped**.

## Defect classification

### A — blocking production defect

None found.

### B — required production hardening

None found.

### Test / acceptance harness corrections

Deterministically corrected and rerun:

- future fake-clock timestamp conflicting with repository-owned UTC cleanup timestamps;
- inputs intended to arrive after restart accidentally visible to startup recovery;
- invalid `list_ready_for_client(limit=1000)` oracle exceeding the repository's public max `200`;
- inefficient repeated READY-index oracle scans (reduced to one bounded scan per generated step without changing invariants);
- process-global test composition leakage for legacy standalone output tests;
- synthetic Telegram host discovery depending on full-suite import order;
- full-repository import-time configuration ordering.

### C — deferred / outside v0.4-input-runtime

- Telegram edited-message history rewind
- PostgreSQL / SQLAlchemy / Alembic
- Redis
- distributed workers / leases
- distributed runtime
- scheduler
- `AgentRun` / `TaskRun`
- parallel branches
- fork/join
- new durable global event bus

### D — cosmetic

No cosmetic issue blocks acceptance.

## Maintainer live gate — PENDING

Live acceptance was **not** executed by GitHub Actions and was **not** simulated as a substitute for real service behavior.

The maintainer must test the exact automated head SHA `8a299202e7a9e5040c9b463a2813aabf99e4612a` (or, if a report-only commit is checked out, verify production/test/workflow code is unchanged from that code SHA), with no uncommitted production changes, preferably in a dedicated test Telegram chat and isolated test storage root/profile.

Required scenarios:

1. **Real E2E basic** — Telegram input → ingress → committed batch → admission → AgentCycle → real LLM → safe read-only MCP/search/read → finalization → exactly one final Telegram output. Check initial acknowledgement, one active cycle, one final delivery, no duplicate.
2. **`/status`** — during RUNNING and after terminal. It must be read-only, coherent, create no input/control/cycle and not interrupt the task.
3. **Running additions** — send at least two follow-up inputs while one real task is active. Confirm same cycle, FIFO acknowledgements/APPLIED projection and final answer accounting for additions.
4. **`/stop`** — command acceptance must be distinguishable from later durable `PAUSED_BY_USER`. If received during an atomic tool block, the block finishes with valid assistant/tool history and no next semantic block starts.
5. **Input while paused** — ordinary input is durably accepted/queued; cycle stays paused; no implicit continue or runner wake; `/status` reflects it.
6. **`/continue`** — same cycle id; all pre-continue queued input is in the frozen target and applied before the next meaningful resumed LLM step; late post-continue input waits for a later checkpoint.
7. **Semantic AgentEmission** — only if the real production profile exposes the manager `send_user_message` tool. Observe one semantic intermediate Telegram message, no duplicate, cycle continues, final output is separate. If the tool is not exposed, record that factual reason; do not alter prompts/product semantics for the test.
8. **Safe restart** — reach actual `PAUSED_BY_USER`, stop the application, restart with the same durable root, `/status` must show the same paused cycle/context, then `/continue` resumes that same cycle without duplicate input or blind replay.
9. **Optional natural WAITING restart** — only if ordinary production behavior can reproducibly reach `WAITING_USER`: restart, preserve same question/context, reply, `RESUME_WAITING` same cycle. Do not alter production prompts to force this state.

Live safety rules:

- no mutating external MCP tools;
- read/search/retrieve/status only;
- no email send, purchase, calendar mutation, deletion or irreversible side effect;
- no bot token, LLM key, MCP secret, chat id, private endpoint credential, raw private user text, raw prompt or raw tool content in the report;
- record scenario id, exact SHA, PASS/FAIL, concise lifecycle summary, services used and duplicate side-effect count.

If any live scenario fails, IR-10 remains incomplete. Reproduce offline where possible, fix minimally, retain/add regression, obtain a new automated green SHA and rerun the affected live scenarios; live evidence from an older production SHA is invalid after a production fix.

## Automated conclusion

**IR-10 automated acceptance = GREEN.**

**Maintainer live gate = PENDING.**

Therefore this report deliberately does **not** state `IR-10 implemented and validated` and does **not** state `v0.4-input-runtime implemented and validated`. Canonical docs and PR #6 IR-10 checkbox must remain unchanged until the real maintainer live gate passes.
